{
  "name": "@tva-protocol/packages",
  "version": "0.1.0",
  "private": true,
  "description": "TVA Protocol SDK packages",
  "workspaces": [
    "sdk",
    "hardhat-plugin",
    "wallet-adapter"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
